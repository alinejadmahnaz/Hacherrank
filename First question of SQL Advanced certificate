SELECT substring(record_date,6,2) AS [MONTH] , MIN(DATA_VALUE) AS [MIN]
,MAX(DATA_VALUE) AS [MAX] , round(AVG(DATA_VALUE)) AS AVG
FROM TEMPERATURE_RECORDS
WHERE
GROUP BY record_date,DATA_VALUE
ORDER BY MAX(DATA_VALUE) DESC
